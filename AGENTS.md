# Agent Guidelines

## Documentation

All project documentation lives in the `_docs/` directory. When writing documentation (.md files), create or edit files in `_docs/`, not in the project root or elsewhere.
